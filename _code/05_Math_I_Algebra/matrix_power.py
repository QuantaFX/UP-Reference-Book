MOD = 998244353 ------------------------------------------------------
 ---------------------------------------------------------------------
def multiply(A, B): --------------------------------------------------
--- p, q, r = len(A), len(A[0]), len(B[0]) ---------------------------
 ---------------------------------------------------------------------
--- res = [[0] * r for _ in range(p)] --------------------------------
 ---------------------------------------------------------------------
--- for i in range(p): -----------------------------------------------
------- for j in range(q): -------------------------------------------
----------- for k in range(r): ---------------------------------------
--------------- res[i][k] = (res[i][k] + A[i][j] * B[j][k]) % MOD ----
 ---------------------------------------------------------------------
--- return res -------------------------------------------------------
 ---------------------------------------------------------------------
def identity(n): -----------------------------------------------------
--- I = [[0] * n for _ in range(n)] ----------------------------------
--- for i in range(n): -----------------------------------------------
------- I[i][i] = 1 --------------------------------------------------
--- return I ---------------------------------------------------------
 ---------------------------------------------------------------------
def power(B, e): -----------------------------------------------------
--- n = len(B) -------------------------------------------------------
 ---------------------------------------------------------------------
--- result = identity(n) ---------------------------------------------
--- base = [row[:] for row in B] -------------------------------------
 ---------------------------------------------------------------------
--- while e > 0: -----------------------------------------------------
------- if e & 1: ----------------------------------------------------
----------- result = multiply(result, base) --------------------------
------- base = multiply(base, base) ----------------------------------
------- e >>= 1 ------------------------------------------------------
 ---------------------------------------------------------------------
--- return result ----------------------------------------------------
