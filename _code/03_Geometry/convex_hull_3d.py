class P3: ------------------------------------------------------------
--- __slots__ = ['x', 'y', 'z'] --------------------------------------
--- def __init__(self, x=0, y=0, z=0): -------------------------------
------- self.x, self.y, self.z = x, y, z -----------------------------
--- def __sub__(self, o): --------------------------------------------
------- return P3(self.x-o.x, self.y-o.y, self.z-o.z) ----------------
--- def cross(self, o): ----------------------------------------------
------- return P3(self.y*o.z - self.z*o.y, ---------------------------
----------------- self.z*o.x - self.x*o.z, ---------------------------
----------------- self.x*o.y - self.y*o.x) ---------------------------
--- def dot(self, o): ------------------------------------------------
------- return self.x*o.x + self.y*o.y + self.z*o.z ------------------
--- def __eq__(self, o): ---------------------------------------------
------- return (self.x, self.y, self.z) == (o.x, o.y, o.z) -----------
--- def __lt__(self, o): ---------------------------------------------
------- return (self.x, self.y, self.z) < (o.x, o.y, o.z) ------------
 ---------------------------------------------------------------------
class Face: ----------------------------------------------------------
--- __slots__ = ['p', 'q'] -------------------------------------------
--- def __init__(self, p, q): ----------------------------------------
------- self.p = p # Tuple of 3 point indices ------------------------
------- self.q = q # Normal vector (P3 object) -----------------------
 ---------------------------------------------------------------------
def hull_3D(pts): ----------------------------------------------------
--- n = len(pts) -----------------------------------------------------
--- faces = [] -------------------------------------------------------
--- dead = [[True] * n for _ in range(n)] ----------------------------
--- def add_face(a, b, c): -------------------------------------------
------- q = (pts[b] - pts[a]).cross(pts[c] - pts[a]) -----------------
------- faces.append(Face((a, b, c), q)) -----------------------------
------- dead[a][b] = dead[b][c] = dead[c][a] = False -----------------
--- # Initialize with a degenerate tetrahedron -----------------------
--- add_face(0, 1, 2) ------------------------------------------------
--- add_face(0, 2, 1) ------------------------------------------------
--- for i in range(3, n): --------------------------------------------
------- f_inv = [] ---------------------------------------------------
------- for f in faces: ----------------------------------------------
----------- # If face is visible from the point ----------------------
----------- if (pts[i] - pts[f.p[0]]).dot(f.q) > 0: ------------------
--------------- for j in range(3): -----------------------------------
------------------- dead[f.p[j]][f.p[(j+1)%3]] = True ----------------
----------- else: ----------------------------------------------------
--------------- f_inv.append(f) --------------------------------------
------- faces = [] ---------------------------------------------------
------- for f in f_inv: ----------------------------------------------
----------- for j in range(3): ---------------------------------------
--------------- a, b = f.p[j], f.p[(j+1)%3] --------------------------
--------------- # If reverse edge is dead but forward is alive -------
--------------- if dead[b][a]: ---------------------------------------
------------------- add_face(b, a, i) --------------------------------
------- faces.extend(f_inv) ------------------------------------------
--- return faces -----------------------------------------------------
