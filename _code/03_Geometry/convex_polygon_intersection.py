EPS = 1e-9 -----------------------------------------------------------
 ---------------------------------------------------------------------
def sub(a, b): -------------------------------------------------------
--- return (a[0] - b[0], a[1] - b[1]) --------------------------------
def add(a, b): -------------------------------------------------------
--- return (a[0] + b[0], a[1] + b[1]) --------------------------------
def cross(a, b): -----------------------------------------------------
--- return a[0] * b[1] - a[1] * b[0] ---------------------------------
def dot(a, b): -------------------------------------------------------
--- return a[0] * b[0] + a[1] * b[1] ---------------------------------
def orient(a, b, c): -------------------------------------------------
--- return cross(sub(b, a), sub(c, a)) -------------------------------
def seg_inter(a, b, c, d): -------------------------------------------
--- def on_seg(p, a, b): ---------------------------------------------
------- return ( -----------------------------------------------------
----------- abs(orient(a, b, p)) < EPS -------------------------------
----------- and min(a[0], b[0]) - EPS <= p[0] <= max(a[0], b[0]) + EPS
----------- and min(a[1], b[1]) - EPS <= p[1] <= max(a[1], b[1]) + EPS
------- ) ------------------------------------------------------------
--- def line_inter(a, b, c, d): --------------------------------------
------- A1 = b[1] - a[1] ---------------------------------------------
------- B1 = a[0] - b[0] ---------------------------------------------
------- C1 = A1 * a[0] + B1 * a[1] -----------------------------------
------- A2 = d[1] - c[1] ---------------------------------------------
------- B2 = c[0] - d[0] ---------------------------------------------
------- C2 = A2 * c[0] + B2 * c[1] -----------------------------------
------- det = A1 * B2 - A2 * B1 --------------------------------------
------- if abs(det) < EPS: -------------------------------------------
----------- return None ----------------------------------------------
------- x = (B2 * C1 - B1 * C2) / det --------------------------------
------- y = (A1 * C2 - A2 * C1) / det --------------------------------
------- return (x, y) ------------------------------------------------
--- p = line_inter(a, b, c, d) ---------------------------------------
--- if p is None: ----------------------------------------------------
------- return None --------------------------------------------------
--- if ( -------------------------------------------------------------
------- on_seg(p, a, b) ----------------------------------------------
------- and on_seg(p, c, d) ------------------------------------------
--- ): ---------------------------------------------------------------
------- return p -----------------------------------------------------
--- return None ------------------------------------------------------
def in_poly(p, poly): ------------------------------------------------
--- inside = False ---------------------------------------------------
--- n = len(poly) ----------------------------------------------------
--- for i in range(n): -----------------------------------------------
------- j = (i - 1) % n ----------------------------------------------
------- a, b = poly[i], poly[j] --------------------------------------
------- if ((a[1] > p[1]) != (b[1] > p[1])): -------------------------
----------- x = (b[0] - a[0]) * (p[1] - a[1]) / (b[1] - a[1] + EPS) + a[0]
----------- if p[0] < x: ---------------------------------------------
--------------- inside = not inside ----------------------------------
--- return inside ----------------------------------------------------
def on_poly(p, poly): ------------------------------------------------
--- n = len(poly) ----------------------------------------------------
--- for i in range(n): -----------------------------------------------
------- j = (i - 1) % n ----------------------------------------------
------- if abs(orient(poly[j], poly[i], p)) < EPS: -------------------
----------- return True ----------------------------------------------
--- return False -----------------------------------------------------
def convex_hull(points): ---------------------------------------------
--- points = sorted(points) ------------------------------------------
--- def build(h): ----------------------------------------------------
------- for p in points: ---------------------------------------------
----------- while len(h) >= 2 and orient(h[-2], h[-1], p) <= EPS: ----
--------------- h.pop() ----------------------------------------------
----------- h.append(p) ----------------------------------------------
------- return h -----------------------------------------------------
--- lower = build([]) ------------------------------------------------
--- upper = build([]) ------------------------------------------------
--- return lower[:-1] + upper[:-1] -----------------------------------
def convex_polygon_inter(a, b): --------------------------------------
--- ans = [] ---------------------------------------------------------
--- for p in a: ------------------------------------------------------
------- if in_poly(p, b) or on_poly(p, b): ---------------------------
----------- ans.append(p) --------------------------------------------
--- for p in b: ------------------------------------------------------
------- if in_poly(p, a) or on_poly(p, a): ---------------------------
----------- ans.append(p) --------------------------------------------
--- for i in range(len(a)): ------------------------------------------
------- I = (i - 1) % len(a) -----------------------------------------
------- for j in range(len(b)): --------------------------------------
----------- J = (j - 1) % len(b) -------------------------------------
----------- p = seg_inter(a[I], a[i], b[J], b[j]) --------------------
----------- if p is not None: ----------------------------------------
--------------- ans.append(p) ----------------------------------------
--- if not ans: ------------------------------------------------------
------- return [] ----------------------------------------------------
--- return convex_hull(ans) ------------------------------------------
