import math ----------------------------------------------------------
 ---------------------------------------------------------------------
PI = math.pi ---------------------------------------------------------
 ---------------------------------------------------------------------
def clamp(x, lo, hi): ------------------------------------------------
--- return max(lo, min(hi, x)) ---------------------------------------
 ---------------------------------------------------------------------
# ========================================================= ----------
# version 1: lat/long (spherical law of cosines) ---------------------
# ========================================================= ----------
def great_circle_dist_latlong(lat1, lon1, lat2, lon2, R=6371.0): -----
--- lat1 *= PI / 180 -------------------------------------------------
--- lon1 *= PI / 180 -------------------------------------------------
--- lat2 *= PI / 180 -------------------------------------------------
--- lon2 *= PI / 180 -------------------------------------------------
 ---------------------------------------------------------------------
--- val = ( ----------------------------------------------------------
------- math.sin(lat1) * math.sin(lat2) ------------------------------
------- + math.cos(lat1) * math.cos(lat2) * math.cos(abs(lon1 - lon2))
--- ) ----------------------------------------------------------------
 ---------------------------------------------------------------------
--- val = clamp(val, -1.0, 1.0)  # IMPORTANT FIX ---------------------
 ---------------------------------------------------------------------
--- return R * math.acos(val) ----------------------------------------
 ---------------------------------------------------------------------
def dot(a, b): -------------------------------------------------------
--- return sum(x * y for x, y in zip(a, b)) --------------------------
 ---------------------------------------------------------------------
def cross(a, b): -----------------------------------------------------
--- return [ ---------------------------------------------------------
------- a[1] * b[2] - a[2] * b[1], -----------------------------------
------- a[2] * b[0] - a[0] * b[2], -----------------------------------
------- a[0] * b[1] - a[1] * b[0], -----------------------------------
--- ] ----------------------------------------------------------------
 ---------------------------------------------------------------------
def norm(v): ---------------------------------------------------------
--- return math.sqrt(dot(v, v)) --------------------------------------
 ---------------------------------------------------------------------
# ========================================================= ----------
# version 2: using 3D vectors (angle between vectors) ----------------
# ========================================================= ----------
def great_circle_dist_vec(a, b, R=1.0): ------------------------------
--- # assumes a and b are unit vectors on sphere ---------------------
--- cross_prod = cross(a, b) -----------------------------------------
--- return R * math.atan2(norm(cross_prod), dot(a, b)) ---------------
 ---------------------------------------------------------------------
def to_vec(lat, lon): ------------------------------------------------
--- lat *= PI / 180 --------------------------------------------------
--- lon *= PI / 180 --------------------------------------------------
 ---------------------------------------------------------------------
--- return [ ---------------------------------------------------------
------- math.cos(lat) * math.cos(lon), -------------------------------
------- math.cos(lat) * math.sin(lon), -------------------------------
------- math.sin(lat), -----------------------------------------------
--- ] ----------------------------------------------------------------
