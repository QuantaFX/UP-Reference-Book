from collections import defaultdict ----------------------------------
 ---------------------------------------------------------------------
class DirectedEuler: -------------------------------------------------
--- def __init__(self, n): -------------------------------------------
------- self.n = n ---------------------------------------------------
------- self.adj = defaultdict(list) ---------------------------------
------- self.indeg = [0]*n -------------------------------------------
------- self.outdeg = [0]*n ------------------------------------------
------- self.edge_count = 0 ------------------------------------------
 ---------------------------------------------------------------------
--- def add_edge(self, u, v): ----------------------------------------
------- self.adj[u].append(v) ----------------------------------------
------- self.outdeg[u] += 1 ------------------------------------------
------- self.indeg[v] += 1 -------------------------------------------
------- self.edge_count += 1 -----------------------------------------
 ---------------------------------------------------------------------
--- def find_start_node(self): ---------------------------------------
------- start = -1 ---------------------------------------------------
------- end = -1 -----------------------------------------------------
------- for i in range(self.n): --------------------------------------
----------- if self.outdeg[i] - self.indeg[i] == 1: ------------------
--------------- if start != -1: --------------------------------------
------------------- return -1 ----------------------------------------
--------------- start = i --------------------------------------------
----------- elif self.indeg[i] - self.outdeg[i] == 1: ----------------
--------------- if end != -1: ----------------------------------------
------------------- return -1 ----------------------------------------
--------------- end = i ----------------------------------------------
----------- elif self.indeg[i] != self.outdeg[i]: --------------------
--------------- return -1 --------------------------------------------
 ---------------------------------------------------------------------
------- if start == -1: ----------------------------------------------
----------- for i in range(self.n): ----------------------------------
--------------- if self.outdeg[i] > 0: -------------------------------
------------------- return i -----------------------------------------
------- return start -------------------------------------------------
 ---------------------------------------------------------------------
--- def get_euler_path(self): ----------------------------------------
------- start = self.find_start_node() -------------------------------
------- if start == -1: ----------------------------------------------
----------- return None ----------------------------------------------
 ---------------------------------------------------------------------
------- stack = [start] ----------------------------------------------
------- path = [] ----------------------------------------------------
 ---------------------------------------------------------------------
------- local_adj = {u: self.adj[u][:] for u in self.adj} ------------
 ---------------------------------------------------------------------
------- while stack: -------------------------------------------------
----------- u = stack[-1] --------------------------------------------
----------- if u in local_adj and local_adj[u]: ----------------------
--------------- v = local_adj[u].pop() -------------------------------
--------------- stack.append(v) --------------------------------------
----------- else: ----------------------------------------------------
--------------- path.append(stack.pop()) -----------------------------
 ---------------------------------------------------------------------
------- if len(path) != self.edge_count + 1: -------------------------
----------- return None ----------------------------------------------
 ---------------------------------------------------------------------
------- return path[::-1] --------------------------------------------
 ---------------------------------------------------------------------
 ---------------------------------------------------------------------
 ---------------------------------------------------------------------
--- class UndirectedEuler: -------------------------------------------
------- def __init__(self, n): ---------------------------------------
----------- self.n = n -----------------------------------------------
----------- self.adj = defaultdict(list) -----------------------------
----------- self.edge_count = 0 --------------------------------------
 ---------------------------------------------------------------------
------- def add_edge(self, u, v): ------------------------------------
----------- self.adj[u].append(v) ------------------------------------
----------- self.adj[v].append(u) ------------------------------------
----------- self.edge_count += 1 -------------------------------------
 ---------------------------------------------------------------------
------- def is_valid(self): ------------------------------------------
----------- odd = 0 --------------------------------------------------
----------- for i in range(self.n): ----------------------------------
--------------- if len(self.adj[i]) % 2 != 0: ------------------------
------------------- odd += 1 -----------------------------------------
----------- return odd == 0 or odd == 2 ------------------------------
 ---------------------------------------------------------------------
------- def get_euler_path(self): ------------------------------------
----------- if not self.is_valid(): ----------------------------------
--------------- return None ------------------------------------------
 ---------------------------------------------------------------------
----------- start = 0 ------------------------------------------------
----------- for i in range(self.n): ----------------------------------
--------------- if len(self.adj[i]) % 2 == 1: ------------------------
------------------- start = i ----------------------------------------
------------------- break --------------------------------------------
--------------- if len(self.adj[i]) > 0: -----------------------------
------------------- start = i ----------------------------------------
 ---------------------------------------------------------------------
----------- stack = [start] ------------------------------------------
----------- path = [] ------------------------------------------------
 ---------------------------------------------------------------------
----------- local_adj = {u: self.adj[u][:] for u in self.adj} --------
 ---------------------------------------------------------------------
----------- while stack: ---------------------------------------------
--------------- u = stack[-1] ----------------------------------------
--------------- if u in local_adj and local_adj[u]: ------------------
------------------- v = local_adj[u].pop() ---------------------------
------------------- local_adj[v].remove(u) ---------------------------
------------------- stack.append(v) ----------------------------------
--------------- else: ------------------------------------------------
------------------- path.append(stack.pop()) -------------------------
 ---------------------------------------------------------------------
----------- if len(path) != self.edge_count + 1: ---------------------
--------------- return None ------------------------------------------
 ---------------------------------------------------------------------
----------- return path[::-1] ----------------------------------------
