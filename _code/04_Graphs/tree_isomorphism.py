from collections import deque ----------------------------------------
 ---------------------------------------------------------------------
def bfs(u, adj, n): --------------------------------------------------
--- vis = [False] * n ------------------------------------------------
--- pre = [-1] * n ---------------------------------------------------
--- q = deque() ------------------------------------------------------
 ---------------------------------------------------------------------
--- q.append(u) ------------------------------------------------------
--- vis[u] = True ----------------------------------------------------
 ---------------------------------------------------------------------
--- last = u ---------------------------------------------------------
 ---------------------------------------------------------------------
--- while q: ---------------------------------------------------------
------- u = q.popleft() ----------------------------------------------
------- last = u -----------------------------------------------------
 ---------------------------------------------------------------------
------- for v in adj[u]: ---------------------------------------------
----------- if not vis[v]: -------------------------------------------
--------------- vis[v] = True ----------------------------------------
--------------- pre[v] = u -------------------------------------------
--------------- q.append(v) ------------------------------------------
 ---------------------------------------------------------------------
--- return last, pre -------------------------------------------------
 ---------------------------------------------------------------------
def tree_centers(r, adj, n): -----------------------------------------
--- u, _ = bfs(r, adj, n) --------------------------------------------
--- v, pre = bfs(u, adj, n) ------------------------------------------
 ---------------------------------------------------------------------
--- path = [] --------------------------------------------------------
--- while v != -1: ---------------------------------------------------
------- path.append(v) -----------------------------------------------
------- v = pre[v] ---------------------------------------------------
 ---------------------------------------------------------------------
--- size = len(path) -------------------------------------------------
 ---------------------------------------------------------------------
--- med = [path[size // 2]] ------------------------------------------
--- if size % 2 == 0: ------------------------------------------------
------- med.append(path[size // 2 - 1]) ------------------------------
 ---------------------------------------------------------------------
--- return med -------------------------------------------------------
 ---------------------------------------------------------------------
def rootcode(u, adj, p, d, pr): --------------------------------------
--- children = [] ----------------------------------------------------
--- for v in adj[u]: -------------------------------------------------
------- if v != p: ---------------------------------------------------
----------- children.append(rootcode(v, adj, u, (d + 1) % len(pr), pr))
 ---------------------------------------------------------------------
--- children.sort() --------------------------------------------------
 ---------------------------------------------------------------------
--- h = len(children) + 1 --------------------------------------------
--- for x in children: -----------------------------------------------
------- h = h * pr[d] + x --------------------------------------------
 ---------------------------------------------------------------------
--- return h ---------------------------------------------------------
 ---------------------------------------------------------------------
def treecode(root, adj, n, pr): --------------------------------------
--- c = tree_centers(root, adj, n) -----------------------------------
 ---------------------------------------------------------------------
--- if len(c) == 1: --------------------------------------------------
------- return (rootcode(c[0], adj, -1, 0, pr) << 1) | 1 -------------
--- else: ------------------------------------------------------------
------- return (rootcode(c[0], adj, -1, 0, pr) * ---------------------
--------------- rootcode(c[1], adj, -1, 0, pr)) << 1 -----------------
 ---------------------------------------------------------------------
def isomorphic(r1, adj1, r2, adj2, n1, n2, pr, rooted=False): --------
--- if rooted: -------------------------------------------------------
------- return rootcode(r1, adj1, -1, 0, pr) == rootcode(r2, adj2, -1, 0, pr)
--- return treecode(r1, adj1, n1, pr) == treecode(r2, adj2, n2, pr) --
