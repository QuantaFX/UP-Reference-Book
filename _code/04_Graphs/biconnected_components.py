class BiconnectedComponents: -----------------------------------------
--- def __init__(self, n): -------------------------------------------
------- self.n = n ---------------------------------------------------
------- self.graph = [[] for _ in range(n)] --------------------------
------- self.timer = 0 -----------------------------------------------
------- self.tin = [-1] * n ------------------------------------------
------- self.low = [-1] * n ------------------------------------------
------- self.stack = [] ----------------------------------------------
------- self.bcc = [] ------------------------------------------------
 ---------------------------------------------------------------------
--- def add_edge(self, u, v): ----------------------------------------
------- self.graph[u].append(v) --------------------------------------
------- self.graph[v].append(u) --------------------------------------
 ---------------------------------------------------------------------
--- def dfs(self, v, parent): ----------------------------------------
------- self.tin[v] = self.low[v] = self.timer -----------------------
------- self.timer += 1 ----------------------------------------------
 ---------------------------------------------------------------------
------- for to in self.graph[v]: -------------------------------------
----------- if to == parent: -----------------------------------------
--------------- continue ---------------------------------------------
 ---------------------------------------------------------------------
----------- if self.tin[to] == -1: -----------------------------------
 ---------------------------------------------------------------------
--------------- self.stack.append((v, to)) ---------------------------
--------------- self.dfs(to, v) --------------------------------------
--------------- self.low[v] = min(self.low[v], self.low[to]) ---------
 ---------------------------------------------------------------------
--------------- if self.low[to] >= self.tin[v]: ----------------------
------------------- component = [] -----------------------------------
------------------- while True: --------------------------------------
----------------------- e = self.stack.pop() -------------------------
----------------------- component.append(e) --------------------------
----------------------- if e == (v, to): -----------------------------
--------------------------- break ------------------------------------
------------------- self.bcc.append(component) -----------------------
 ---------------------------------------------------------------------
----------- elif self.tin[to] < self.tin[v]: -------------------------
--------------- # back edge ------------------------------------------
--------------- self.low[v] = min(self.low[v], self.tin[to]) ---------
--------------- self.stack.append((v, to)) ---------------------------
 ---------------------------------------------------------------------
--- def find_bcc(self): ----------------------------------------------
------- for i in range(self.n): --------------------------------------
----------- if self.tin[i] == -1: ------------------------------------
--------------- self.dfs(i, -1) --------------------------------------
------- return self.bcc ----------------------------------------------
