class SegmentTree: ---------------------------------------------------
--- def __init__(self, n): -------------------------------------------
------- self.n = n ---------------------------------------------------
------- self.tree = [0] * (4 * n) ------------------------------------
 ---------------------------------------------------------------------
--- # Example: point update ------------------------------------------
--- def update(self, idx, val, node=1, l=0, r=None): -----------------
------- if r is None: ------------------------------------------------
----------- r = self.n - 1 -------------------------------------------
------- if l == r: ---------------------------------------------------
----------- self.tree[node] = vala -----------------------------------
----------- return ---------------------------------------------------
------- mid = (l + r) // 2 -------------------------------------------
------- if idx <= mid: -----------------------------------------------
----------- self.update(idx, val, node * 2, l, mid) ------------------
------- else: --------------------------------------------------------
----------- self.update(idx, val, node * 2 + 1, mid + 1, r) ----------
------- self.tree[node] = self.tree[node * 2] + self.tree[node * 2 + 1]
 ---------------------------------------------------------------------
--- # Example: range query -------------------------------------------
--- def query(self, ql, qr, node=1, l=0, r=None): --------------------
------- if r is None: ------------------------------------------------
----------- r = self.n - 1 -------------------------------------------
------- if qr < l or r < ql: -----------------------------------------
----------- return 0 -------------------------------------------------
------- if ql <= l and r <= qr: --------------------------------------
----------- return self.tree[node] -----------------------------------
------- mid = (l + r) // 2 -------------------------------------------
------- return self.query(ql, qr, node * 2, l, mid) + \ --------------
-------------- self.query(ql, qr, node * 2 + 1, mid + 1, r) ----------
 ---------------------------------------------------------------------
 ---------------------------------------------------------------------
class HeavyLightTree: ------------------------------------------------
--- def __init__(self, n): -------------------------------------------
------- self.n = n ---------------------------------------------------
------- self.adj = [[] for _ in range(n)] ----------------------------
 ---------------------------------------------------------------------
------- self.par = [-1] * n ------------------------------------------
------- self.heavy = [-1] * n ----------------------------------------
------- self.dep = [0] * n -------------------------------------------
------- self.path_root = [0] * n -------------------------------------
------- self.pos = [0] * n -------------------------------------------
 ---------------------------------------------------------------------
------- self.segment_tree = SegmentTree(n) ---------------------------
------- self.current_pos = 0 -----------------------------------------
 ---------------------------------------------------------------------
--- def add_edge(self, u, v): ----------------------------------------
------- self.adj[u].append(v) ----------------------------------------
------- self.adj[v].append(u) ----------------------------------------
 ---------------------------------------------------------------------
--- def dfs(self, u): ------------------------------------------------
------- size = 1 -----------------------------------------------------
------- max_subtree_size = 0 -----------------------------------------
 ---------------------------------------------------------------------
------- for v in self.adj[u]: ----------------------------------------
----------- if v != self.par[u]: -------------------------------------
--------------- self.par[v] = u --------------------------------------
--------------- self.dep[v] = self.dep[u] + 1 ------------------------
--------------- subtree_size = self.dfs(v) ---------------------------
 ---------------------------------------------------------------------
--------------- if subtree_size > max_subtree_size: ------------------
------------------- max_subtree_size = subtree_size ------------------
------------------- self.heavy[u] = v --------------------------------
 ---------------------------------------------------------------------
--------------- size += subtree_size ---------------------------------
 ---------------------------------------------------------------------
------- return size --------------------------------------------------
 ---------------------------------------------------------------------
--- def decompose(self, u, root): ------------------------------------
------- self.path_root[u] = root -------------------------------------
------- self.pos[u] = self.current_pos -------------------------------
------- self.current_pos += 1 ----------------------------------------
 ---------------------------------------------------------------------
------- if self.heavy[u] != -1: --------------------------------------
----------- self.decompose(self.heavy[u], root) ----------------------
 ---------------------------------------------------------------------
------- for v in self.adj[u]: ----------------------------------------
----------- if v != self.par[u] and v != self.heavy[u]: --------------
--------------- self.decompose(v, v) ---------------------------------
 ---------------------------------------------------------------------
--- def build(self, root=0): -----------------------------------------
------- self.par[root] = root ----------------------------------------
------- self.dep[root] = 0 -------------------------------------------
------- self.dfs(root) -----------------------------------------------
------- self.decompose(root, root) -----------------------------------
 ---------------------------------------------------------------------
--- # Query path u -> v ----------------------------------------------
--- def query(self, u, v): -------------------------------------------
------- res = 0 ------------------------------------------------------
------- while self.path_root[u] != self.path_root[v]: ----------------
----------- if self.dep[self.path_root[u]] < self.dep[self.path_root[v]]:
--------------- u, v = v, u ------------------------------------------
----------- start = self.path_root[u] --------------------------------
----------- res += self.segment_tree.query(self.pos[start], self.pos[u])
----------- u = self.par[start] --------------------------------------
 ---------------------------------------------------------------------
------- # Same path --------------------------------------------------
------- if self.dep[u] > self.dep[v]: --------------------------------
----------- u, v = v, u ----------------------------------------------
------- res += self.segment_tree.query(self.pos[u], self.pos[v]) -----
------- return res ---------------------------------------------------
 ---------------------------------------------------------------------
--- # Update node value ----------------------------------------------
--- def update(self, u, value): --------------------------------------
------- self.segment_tree.update(self.pos[u], value) -----------------
