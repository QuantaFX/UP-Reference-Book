from collections import deque ----------------------------------------
import random --------------------------------------------------------
import string --------------------------------------------------------
 ---------------------------------------------------------------------
class Node: ----------------------------------------------------------
--- def __init__(self): ----------------------------------------------
------- self.next = {} -----------------------------------------------
------- self.fail = None ---------------------------------------------
------- self.count = 0 -----------------------------------------------
 ---------------------------------------------------------------------
--- def add(self, s): ------------------------------------------------
------- node = self --------------------------------------------------
------- for c in s: --------------------------------------------------
----------- if c not in node.next: -----------------------------------
--------------- node.next[c] = Node() --------------------------------
----------- node = node.next[c] --------------------------------------
------- node.count += 1 ----------------------------------------------
 ---------------------------------------------------------------------
--- def prepare(self): -----------------------------------------------
------- root = self --------------------------------------------------
------- q = deque() --------------------------------------------------
 ---------------------------------------------------------------------
------- root.fail = root ---------------------------------------------
 ---------------------------------------------------------------------
------- for child in root.next.values(): -----------------------------
----------- child.fail = root ----------------------------------------
----------- q.append(child) ------------------------------------------
 ---------------------------------------------------------------------
------- while q: -----------------------------------------------------
----------- head = q.popleft() ---------------------------------------
 ---------------------------------------------------------------------
----------- for c, nxt in head.next.items(): -------------------------
--------------- f = head.fail ----------------------------------------
 ---------------------------------------------------------------------
--------------- while f != root and c not in f.next: -----------------
------------------- f = f.fail ---------------------------------------
 ---------------------------------------------------------------------
--------------- nxt.fail = f.next[c] if c in f.next else root --------
 ---------------------------------------------------------------------
--------------- nxt.count += nxt.fail.count --------------------------
 ---------------------------------------------------------------------
--------------- q.append(nxt) ----------------------------------------
 ---------------------------------------------------------------------
--- def search(self, s): ---------------------------------------------
------- root = self --------------------------------------------------
------- p = self -----------------------------------------------------
------- ans = 0 ------------------------------------------------------
 ---------------------------------------------------------------------
------- for c in s: --------------------------------------------------
----------- while p != root and c not in p.next: ---------------------
--------------- p = p.fail -------------------------------------------
 ---------------------------------------------------------------------
----------- if c in p.next: ------------------------------------------
--------------- p = p.next[c] ----------------------------------------
--------------- ans += p.count ---------------------------------------
 ---------------------------------------------------------------------
------- return ans ---------------------------------------------------
