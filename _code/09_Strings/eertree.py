class Node: ----------------------------------------------------------
--- def __init__(self, start=0, end=0, length=0, link=0): ------------
------- self.start = start -------------------------------------------
------- self.end = end -----------------------------------------------
------- self.len = length --------------------------------------------
------- self.link = link ---------------------------------------------
------- self.next = [0] * 26 -----------------------------------------
 ---------------------------------------------------------------------
 ---------------------------------------------------------------------
class Eertree: -------------------------------------------------------
--- def __init__(self): ----------------------------------------------
------- self.tree = [] -----------------------------------------------
 ---------------------------------------------------------------------
------- self.tree.append(Node()) -------------------------------------
 ---------------------------------------------------------------------
------- self.tree.append(Node(0, 0, -1, 1)) --------------------------
 ---------------------------------------------------------------------
------- self.tree.append(Node(0, 0, 0, 1)) ---------------------------
 ---------------------------------------------------------------------
------- self.cur = 1 -------------------------------------------------
------- self.ptr = 2 -------------------------------------------------
 ---------------------------------------------------------------------
--- def get_link(self, v, s, i): -------------------------------------
------- while True: --------------------------------------------------
----------- cur_len = self.tree[v].len -------------------------------
----------- if i - cur_len - 1 >= 0 and s[i] == s[i - cur_len - 1]: --
--------------- return v ---------------------------------------------
----------- v = self.tree[v].link ------------------------------------
 ---------------------------------------------------------------------
--- def add_char(self, s, i): ----------------------------------------
------- cur = self.cur -----------------------------------------------
------- cur = self.get_link(cur, s, i) -------------------------------
 ---------------------------------------------------------------------
------- c = ord(s[i]) - ord('a') -------------------------------------
 ---------------------------------------------------------------------
------- if self.tree[cur].next[c]: -----------------------------------
----------- self.cur = self.tree[cur].next[c] ------------------------
----------- return ---------------------------------------------------
 ---------------------------------------------------------------------
------- self.ptr += 1 ------------------------------------------------
------- self.tree[cur].next[c] = self.ptr ----------------------------
 ---------------------------------------------------------------------
------- length = self.tree[cur].len + 2 ------------------------------
------- self.tree.append(Node(i - length + 1, i, length, 0)) ---------
 ---------------------------------------------------------------------
------- new_node = self.ptr ------------------------------------------
 ---------------------------------------------------------------------
------- if self.tree[new_node].len == 1: -----------------------------
----------- self.tree[new_node].link = 2 -----------------------------
------- else: --------------------------------------------------------
----------- tmp = self.tree[cur].link --------------------------------
----------- tmp = self.get_link(tmp, s, i) ---------------------------
----------- self.tree[new_node].link = self.tree[tmp].next[c] --------
 ---------------------------------------------------------------------
------- self.cur = new_node ------------------------------------------
 ---------------------------------------------------------------------
--- def build(self, s): ----------------------------------------------
------- for i in range(len(s)): --------------------------------------
----------- self.add_char(s, i) --------------------------------------
